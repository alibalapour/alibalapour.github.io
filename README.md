# Ali Balapour

Personal academic website built with Hugo.

## Local development

```sh
hugo server
```

The root `index.html` mirrors the Hugo homepage so the existing GitHub Pages
deployment continues to work without a configuration change.
